[build]
command = "cd frontend && npm ci && npm run build"
publish = "frontend/.next"

[env]
NODE_VERSION = "18.17.0"
NODE_ENV = "production"

[[redirects]]
from = "/*"
status = 200
to = "/index.html"
